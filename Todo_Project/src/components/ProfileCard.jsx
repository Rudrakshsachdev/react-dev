const ProfileCard = () => {
    const styles = {
        backgroundColor: 'lightgreen',
        padding: '15px',
        borderRadius: '8px',
    };

    return (
        <div style={styles}>
            <h2>Profile Card</h2>
            <p>This is a simple profile card component in React.</p>
        </div>
    )
}

export default ProfileCard;